import "@/app/globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";
import { DM_Serif_Display } from "next/font/google";
import { FathomAnalytics } from "@/components/Fathom";

const dmSerif = DM_Serif_Display({
  variable: "--font-header",
  subsets: ["latin"],
  weight: "400",
});

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={`${dmSerif.variable} antialiased`}>
        <FathomAnalytics />
        <Header />
        <div className="fixed inset-0 -z-10 h-full w-full bg-[url(/placeholder.jpg)] bg-cover bg-center bg-no-repeat"></div>
        <div className="w-full px-4 py-5">
          <main className="mt-20 bg-gray-100 min-h-screen shadow-lg rounded-t-lg max-w-[1400px] mx-auto px-5 lg:px-7 py-10">
            {children}
          </main>
          <Footer />
        </div>
      </body>
    </html>
  );
}
